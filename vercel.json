{
  "buildCommand": "PNPM_FROZEN_LOCKFILE=true NODE_ENV=production pnpm vercel-build",
  "installCommand": "PNPM_FROZEN_LOCKFILE=true pnpm install",
  "env": {
    "NODE_ENV": "production",
    "VITE_USER_NODE_ENV": "production"
  },
  "github": {
    "enabled": true,
    "silent": false
  }
}